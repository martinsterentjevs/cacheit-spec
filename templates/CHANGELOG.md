# Changelog

All notable changes to this project are documented in this file.

Format based on [Keep a Changelog](https://keepachangelog.com/),
versioning follows [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added

### Changed

### Fixed

### Removed

<!--
Section reference:
  Added      — new features, new endpoints, new screens
  Changed    — changes to existing behaviour, including performance
  Deprecated — features still present but scheduled for removal
  Removed    — features or endpoints removed
  Fixed      — bug fixes
  Security   — vulnerability fixes, always called out explicitly,
               never buried under Fixed

Entry style:
  - Write for a user or integrator, not a developer reading the diff
  - Lead with what changed, not the ticket number
  - Reference an issue in parentheses if useful:
      "Fixed sync crash on token expiry (#12)"
  - Mark breaking changes clearly:
      - **BREAKING:** device tokens issued before v1.2.0 are invalidated on upgrade

Update [Unreleased] as part of the PR that makes the change —
not reconstructed from commit history at release time.

At release time:
  1. Rename [Unreleased] to [X.Y.Z] - YYYY-MM-DD
  2. Add a fresh empty [Unreleased] section above it
  3. Commit as part of the release branch prep
-->