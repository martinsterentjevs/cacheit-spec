# [Document Title]
Created: {DD/MM/YYYY} Last updated: {DD/MM/YYYY}

## Overview
Short overview of the document content.

## Owns:
- What this document is the source of truth for.

## Does not own:
- What this document is not the source of truth for — link to where it actually lives instead.

## Related documentation:
- Sibling/parent/child docs relevant to this one.
- ADR folder(s) that, if changed, would require updating this doc.

### Content
Content of the topic goes here. Each logical grouping (a DTO, an endpoint category, a key concept) gets its own subheader with its own "Last updated" date, so staleness is visible at the section level, not just the document level.

<!--
Annotation convention: once this doc has an official baseline, a changed rule/field
gets marked inline at the point of change as:
**NEW [date]:** X is now Y — see [decision link]
rather than silently rewritten. Initial versions carry no NEW markers.
-->
